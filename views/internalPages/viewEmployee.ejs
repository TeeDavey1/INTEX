<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Employee</title>
  <link rel="stylesheet" href="css/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: #333;
      color: white;
      padding: 20px 0;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }
    h1 {
      font-size: 2.5rem;
      margin: 0;
      font-weight: 600;
    }
    .container {
      width: 80%;
      margin: 20px auto;
      background-color: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    h2 {
      text-align: center;
      color: #333;
      font-size: 2rem;
    }
    .section {
      margin: 20px 0;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 8px;
      background-color: #f9f9f9;
    }
    .section h3 {
      font-size: 1.5rem;
      color: #333;
      margin-bottom: 10px;
    }
    .info {
      margin: 8px 0;
      font-size: 16px;
      color: #555;
    }
    .info span {
      font-weight: bold;
      color: #333;
    }
    .row {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
    }
    .column {
      flex: 1;
      min-width: 200px;
    }
    button {
      background-color: #228B22;
      color: white;
      border: none;
      padding: 8px 16px;
      cursor: pointer;
      font-size: 14px;
      border-radius: 5px;
    }
    button:hover {
      background-color: #1e7e1e;
    }
    .back-btn {
      display: block;
      width: 200px;
      margin: 20px auto;
      text-align: center;
    }
    .action-buttons {
      display: flex;
      justify-content: center;
      gap: 15px; /* Space between buttons */
      margin-top: 20px;
    }

    .action-buttons a, .action-buttons form {
      display: inline-block;
    }

    button {
      background-color: #228B22;
      color: white;
      border: none;
      padding: 8px 16px;
      cursor: pointer;
      font-size: 14px;
      border-radius: 5px;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #1e7e1e;
    }

    .back-btn {
      display: block;
      width: 200px;
      margin: 20px auto;
      text-align: center;
    }

  </style>
</head>
<body>
  <%- include('../partials/adminHeader') %>
  <header>
    <h1>Employee Details</h1>
  </header>

  <div class="container">
    <h2><%= employee.empfirst %> <%= employee.emplast %></h2>

    <!-- Personal Information and Address Sections Side by Side -->
    <div class="row">
      <div class="column">
        <div class="section">
          <h3>Personal Information</h3>
          <div class="info"><span>Employee ID:</span> <%= employee.empid %></div>
          <div class="info"><span>Phone:</span> <%= employee.empphone %></div>
          <div class="info"><span>Email:</span> <%= employee.empemail %></div>
          <div class="info"><span>Birthday:</span> <%= new Date(employee.empbirthday).toLocaleDateString() %></div>
          <div class="info"><span>Gender:</span> <%= employee.empgender %></div>
          <div class="info"><span>Shirt Size:</span> <%= employee.empshirtsize %></div>
          <div class="info"><span>Permissions:</span> <%= employee.emppermissions %></div>

        </div>
      </div>
      <div class="column">
        <div class="section">
          <h3>Address</h3>
          <div class="info"><span>Street Address:</span> <%= employee.empstreetaddress %></div>
          <div class="info"><span>City:</span> <%= employee.empcity %></div>
          <div class="info"><span>State:</span> <%= employee.empstate %></div>
          <div class="info"><span>Zipcode:</span> <%= employee.empzipcode %></div>
        </div>
      </div>
    </div>

    <!-- Job Information Section Below -->
    <div class="section">
      <h3>Job Information</h3>
      <div class="info"><span>Job Title:</span> <%= employee.empjobtitle %></div>
      <div class="info"><span>Salary:</span> $<%= employee.empsalary %></div>
      <div class="info"><span>Start Date:</span> <%= new Date(employee.empstartdate).toLocaleDateString() %></div>
      <div class="info"><span>Status:</span> <%= employee.empstatus %></div>
      <div class="info"><span>Sewing Level:</span> <%= employee.empsewinglevel %></div>
    </div>

    <div class="action-buttons">
        <a href="/editEmployee/<%= employee.empid %>">
          <button>Edit</button>
        </a>
        <form action="/deleteEmployee/<%= employee.empid %>" method="POST" style="display:inline;" onsubmit="confirmDelete(event)">
          <button type="submit">Delete</button>
        </form>
    </div>

    <!-- Back Button -->
    <div class="back-btn">
      <a href="/maintainEmployees"><button>Back to Employee List</button></a>
    </div>

  </div>

</body>
<script>
    function confirmDelete(event) {
      if (!confirm("Are you sure you want to delete this employee? This action cannot be undone.")) {
        event.preventDefault(); // Prevent form submission if user cancels
      }
    }
</script>  
</html>